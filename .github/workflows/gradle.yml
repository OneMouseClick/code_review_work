name: Java CI with Gradle

on:
  push:
    branches: [ "main", "sharapovdv" ]
  pull_request:
    branches: [ "main", "sharapovdv" ]
  schedule:
    - cron: '30 12 * * 3'
    - cron: '0 22 * * 5'

jobs:
  build:
    strategy:
      matrix:
        # Основные LTS-версии (Temurin)
        java: ['11', '17', '21']
        os: [ubuntu-latest, macos-latest, windows-latest]
        
        # Дополнительные версии (не-LTS)
        include:
          - java: '13'
            os: ubuntu-latest  # Явное указание OS
            distribution: zulu
          - java: '13'
            os: macos-latest   # Полный перебор OS
            distribution: zulu
          - java: '13'
            os: windows-latest
            distribution: zulu
      fail-fast: false

    runs-on: ${{ matrix.os }}  # Теперь корректно разрешается
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: ${{ matrix.distribution || 'temurin' }}

      - name: Build and Test with Gradle
        uses: gradle/gradle-build-action@v3
        with:
          arguments: build test
